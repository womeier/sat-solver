# Rust
/target

# Coq
.*.aux
*.a
*.cma
*.cmi
*.cmo
*.cmx
*.cmxa
*.cmxs
*.glob
*.ml.d
*.ml4.d
*.mli.d
*.mllib.d
*.mlpack.d
*.native
*.o
*.v.d
*.vio
*.vo
*.vos
*.vok
.coq-native/
.csdp.cache
.lia.cache
.nia.cache
.nlia.cache
.nra.cache
csdp.cache
lia.cache
nia.cache
nlia.cache
nra.cache
.Makefile.d
.Makefile.coq.d
Makefile.coq
Makefile.coq.conf

# FStar
.fstar-cache

.claude

# uv-installed tools (lean-lsp-mcp, see .mcp.json / flake.nix shellHook)
/.uv-tools
/.uv-cache

# Project-local toolchain homes (see flake.nix shellHook): $HOME isn't
# executable in some sandboxes, so rustup/cargo/elan are redirected here.
/.rustup-home
/.cargo-home
/.elan-home

# cargo-hax's downloaded charon/aeneas toolchains (see flake.nix shellHook's
# XDG_CACHE_HOME redirect, same $HOME-may-be-noexec reasoning as above).
/.cache

# SATLIB benchmark instances (3000 files, ~2 MB of tarballs): fetched on demand
# by `just satlib`, never committed.
/benchmarks
